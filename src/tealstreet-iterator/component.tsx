import * as React from 'react'

const { hooks } = api

const Component = () => {
  return <div className='p-6 bg-black border'>Build here</div>
}

// For development, will be removed in build
export default Component
